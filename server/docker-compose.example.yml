version: '3.9'

services:
  api:
    build: .
    container_name: motodash-api
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
      CORS_ORIGIN: "*"
      DATABASE_PATH: /app/data/moto-dash.db
    volumes:
      - motodash-data:/app/data

volumes:
  motodash-data:

