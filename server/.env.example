   PORT=4000
   CORS_ORIGIN=*
   DATABASE_PATH=/app/data/moto-dash.db
   ```
   Danach klappt der Build, weil die Datei existiert.

2. **Oder die Copy-Anweisung entfernen**  
   Passe den Dockerfile an und streiche/ersetze die Zeile:
   ```dockerfile
   # COPY .env.example* ./
   ```
   Wenn du später echte Umgebungsvariablen per Stack/Portainer setzt (was empfohlen ist), brauchst du den Copy-Schritt nicht.

Sobald Docker eine passende `.env*`-Datei sieht, verschwindet der Fehler.
